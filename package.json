{
  "name": "aaelink",
  "version": "2.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"bun run dev --filter=@aaelink/backend\" \"bun run dev --filter=@aaelink/frontend\"",
    "build": "bun run build --filter=@aaelink/backend && bun run build --filter=@aaelink/frontend",
    "test": "bun run test --filter=@aaelink/backend && bun run test --filter=@aaelink/frontend",
    "lint": "bun run lint --filter=@aaelink/backend && bun run lint --filter=@aaelink/frontend",
    "typecheck": "cd packages/backend && bun run typecheck && cd ../frontend && bun run typecheck",
    "e2e": "bun run e2e --filter=@aaelink/frontend",
    "clean": "rm -rf node_modules packages/*/node_modules packages/*/dist"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
