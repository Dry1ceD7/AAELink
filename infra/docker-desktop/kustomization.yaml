apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespaces.yaml
  - secrets.example.yaml
  - postgres.yaml
  - redis.yaml
  - mattermost.yaml
  - minio.yaml
  - stirling-pdf.yaml
  # Uncomment if you install NGINX Ingress Controller:
  # - ingress.yaml
