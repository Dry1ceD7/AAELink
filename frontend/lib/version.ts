// Single source of truth for the user-visible application version.
// Keep in sync with frontend/package.json, .env.example (APP_VERSION),
// docker-compose.yml, and the GitHub release tag.
export const APP_VERSION = '0.0.1-alpha'
export const APP_VERSION_LABEL = `v${APP_VERSION}`
